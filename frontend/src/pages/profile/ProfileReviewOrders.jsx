import React from "react";
import AllOrders from "../../components/Shop/AllOrders";

// Review Orders shows orders the seller received (same as seller-orders)
const ProfileReviewOrders = () => {
  return <AllOrders />;
};

export default ProfileReviewOrders;
